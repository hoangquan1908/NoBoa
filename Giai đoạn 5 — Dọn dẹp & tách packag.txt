Giai đoạn 5 — Dọn dẹp & tách packages/shared
Tin tốt: rà soát lại toàn bộ store/ và lib/ — không còn chỗ nào gọi trực tiếp window/document/localStorage (chỉ còn trong hooks/, đúng như dự tính, vì đây là tầng UI sẽ viết lại riêng cho mobile). Vậy nghĩa là code đã đủ "sạch" để tách sang packages/shared.
Bước 1 — Xoá các file rỗng không dùng
bashrm apps/web/src/components/board/items/TextBoxItem.tsx
rm apps/web/src/components/board/items/PhotoItem.tsx
rm apps/web/src/components/board/BoardTabs.tsx
rm apps/web/src/hooks/useAutosave.ts
rm apps/web/src/store/todoStore.ts
rm apps/web/src/pages/SharedBoardPage.tsx
(Đây đều là các "khung" đã tạo sẵn nhưng chức năng thực đã được viết ở nơi khác — theo đúng ghi chú ở Giai đoạn 4.)
Bước 2 — Tạo packages/shared
bashmkdir -p packages/shared/src/{store,lib,types}
packages/shared/package.json:
json{
  "name": "@note-board-app/shared",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "zustand": "^4.5.0",
    "idb": "^8.0.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
Bước 3 — Di chuyển file (giữ nguyên nội dung, chỉ đổi vị trí)
bashgit mv apps/web/src/store/boardStore.ts     packages/shared/src/store/boardStore.ts
git mv apps/web/src/store/notesStore.ts     packages/shared/src/store/notesStore.ts
git mv apps/web/src/store/authStore.ts      packages/shared/src/store/authStore.ts
git mv apps/web/src/lib/indexedDb.ts        packages/shared/src/lib/indexedDb.ts
git mv apps/web/src/lib/supabaseBoardSync.ts packages/shared/src/lib/supabaseBoardSync.ts
git mv apps/web/src/lib/supabaseClient.ts   packages/shared/src/lib/supabaseClient.ts
git mv apps/web/src/lib/utils.ts            packages/shared/src/lib/utils.ts
git mv apps/web/src/types/board.types.ts    packages/shared/src/types/board.types.ts
git mv apps/web/src/types/note.types.ts     packages/shared/src/types/note.types.ts

Giữ lại trong apps/web: mọi thứ trong components/, hooks/, pages/ — đây là tầng UI/DOM, không di chuyển.

Bước 4 — Tạo file export gốc packages/shared/src/index.ts
tsexport * from './store/boardStore';
export * from './store/notesStore';
export * from './store/authStore';
export * from './lib/indexedDb';
export * from './lib/supabaseBoardSync';
export * from './lib/supabaseClient';
export * from './lib/utils';
export * from './types/board.types';
export * from './types/note.types';
Bước 5 — Sửa import trong 21 file còn lại của apps/web
Vì số lượng file khá nhiều, chạy script này để tự động thay from '../../store/...', from '../lib/...', from '../../types/...' (mọi độ sâu ../) thành from '@note-board-app/shared':
bashcd apps/web/src
grep -rl "from ['\"]\(\.\./\)\+\(store\|lib\|types\)/" . | while read -r f; do
  sed -i -E "s#from ['\"](\.\./)+(store|lib|types)/[A-Za-z]+['\"]#from '@note-board-app/shared'#g" "$f"
done
Sau khi chạy, kiểm tra lại thủ công từng file — vì nhiều file import nhiều thứ từ nhiều path khác nhau trên cùng 1 dòng khác nhau, script có thể tạo ra dòng import trùng lặp cần gộp lại bằng tay (ví dụ BoardCanvas.tsx import cả từ store/boardStore lẫn lib/utils lẫn types/board.types — giờ chỉ còn 1 dòng import { ... } from '@note-board-app/shared', cần gộp danh sách tên import).
Bước 6 — Khai báo alias trong apps/web
apps/web/package.json — thêm dependency:
json"dependencies": {
  "@note-board-app/shared": "workspace:*"
}
apps/web/vite.config.ts — không cần thêm gì nếu dùng npm/pnpm workspaces đúng cách (package đã link qua node_modules), nhưng nếu dùng Vite dev server gặp lỗi resolve, thêm:
tsresolve: {
  alias: { '@note-board-app/shared': path.resolve(__dirname, '../../packages/shared/src') }
}
Bước 7 — Test cơ bản cho boardStoreRan 2 commands, created a file, read a fileRan 2 commands, created a file, read a fileCài đặt để chạy được (thêm vào packages/shared/package.json):
json"devDependencies": {
  "vitest": "^2.0.0"
},
"scripts": {
  "test": "vitest run"
}
Chạy: npm --workspace=@note-board-app/shared run test
5 test cover: thêm item, undo, redo, xoá item kèm dây nối liên quan, và undo/redo không crash khi hết lịch sử.

Checklist hoàn thành Giai đoạn 5

 Xoá 6 file rỗng (Bước 1)
 Tạo packages/shared, di chuyển 9 file logic (Bước 2-4)
 Sửa import ở 21 file còn lại trong apps/web (Bước 5) — cần review kỹ bằng tay sau khi chạy script, vì đây là bước dễ gây lỗi build nhất
 Cấu hình workspace + alias (Bước 6)
 Test boardStore chạy pass (Bước 7)
 npm run build ở apps/web không lỗi TypeScript

Khi xong, đối chiếu lại với checklist "điều kiện sẵn sàng chuyển mobile" trong ROADMAP.md (Giai đoạn 6) — nếu tick hết thì đã có thể bắt đầu apps/mobile bằng Expo.



import { describe, it, expect, beforeEach, vi } from 'vitest';

// boardStore ghi xuống IndexedDB (qua lib/indexedDb) mỗi khi state đổi —
// mock lại các hàm này để test chạy được trong môi trường Node (vitest),
// nơi không có IndexedDB thật.
vi.mock('../../lib/indexedDb', () => ({
  saveBoard: vi.fn().mockResolvedValue(undefined),
  getBoard: vi.fn().mockResolvedValue(undefined),
  enqueueSync: vi.fn().mockResolvedValue(undefined),
}));
vi.mock('../../lib/supabaseBoardSync', () => ({
  fetchBoardFromSupabase: vi.fn().mockResolvedValue(null),
}));

import { useBoardStore } from '../boardStore';
import type { Board } from '../../types/board.types';

function freshBoard(): Board {
  const snap = { items: [], connections: [], strokes: [] };
  return { id: 'board-1', name: 'Test Board', snapshot: snap, history: [snap], historyIndex: 0 };
}

describe('boardStore', () => {
  beforeEach(() => {
    useBoardStore.getState().loadBoard(freshBoard());
  });

  it('addSticky thêm 1 item vào snapshot', () => {
    useBoardStore.getState().addSticky(100, 100);
    const { board } = useBoardStore.getState();
    expect(board?.snapshot.items).toHaveLength(1);
    expect(board?.snapshot.items[0].type).toBe('sticky');
  });

  it('undo hoàn tác lại thao tác thêm item', () => {
    useBoardStore.getState().addSticky(100, 100);
    expect(useBoardStore.getState().board?.snapshot.items).toHaveLength(1);

    useBoardStore.getState().undo();
    expect(useBoardStore.getState().board?.snapshot.items).toHaveLength(0);
  });

  it('redo làm lại thao tác vừa undo', () => {
    useBoardStore.getState().addSticky(100, 100);
    useBoardStore.getState().undo();
    useBoardStore.getState().redo();
    expect(useBoardStore.getState().board?.snapshot.items).toHaveLength(1);
  });

  it('deleteItem xoá đúng item và các dây nối liên quan', () => {
    useBoardStore.getState().addSticky(100, 100);
    const id = useBoardStore.getState().board!.snapshot.items[0].id;

    useBoardStore.getState().addSticky(200, 200);
    const id2 = useBoardStore.getState().board!.snapshot.items[1].id;

    useBoardStore.getState().startConnect(id);
    useBoardStore.getState().finishConnect(id2);
    expect(useBoardStore.getState().board?.snapshot.connections).toHaveLength(1);

    useBoardStore.getState().deleteItem(id);
    const { board } = useBoardStore.getState();
    expect(board?.snapshot.items).toHaveLength(1);
    // Dây nối liên quan tới item đã xoá cũng phải biến mất (cascade ở tầng client).
    expect(board?.snapshot.connections).toHaveLength(0);
  });

  it('undo/redo không crash khi không còn lịch sử', () => {
    expect(() => useBoardStore.getState().undo()).not.toThrow();
    expect(() => useBoardStore.getState().redo()).not.toThrow();
  });
});